// 报考专业课程(CourseGrade)表单
export interface ICourseGrade {
  id: string // ID
  name: string // 姓名
  score: string // 姓名
}

// 报考专业(IApplicationInfo)表单
export interface IApplicationInfo {
  id: string // ID
  name: string // 姓名
  major: string // 姓名
  cname: string // 姓名
}
